---
title: Next.js의 라우팅 시스템은 어떻게 동작하나요?
description: 폴더 구조가 곧 URL이 되는 파일 기반 라우팅과 동적 경로, 중첩 레이아웃, 클라이언트 내비게이션을 정리합니다.
category: Next.js 핵심 개념
order: 7
---

import Quiz from '@components/react/Quiz.tsx';

Next.js는 **파일(폴더) 기반 라우팅**을 씁니다. 별도 설정 파일 없이 **폴더 구조가 그대로 URL 경로**가 됩니다. ([앱 라우터 기준](/docs/frontend-tech-interview/33-app-vs-pages-router))

## 폴더 = 경로

`app/` 아래 폴더가 경로 세그먼트가 되고, 그 안의 `page.tsx`가 실제 페이지가 됩니다.

```text
app/
  page.tsx          → /
  about/page.tsx    → /about
  blog/page.tsx     → /blog
```

## 동적 경로

대괄호 폴더로 동적 세그먼트를 만듭니다.

- `[id]` — 단일 동적 세그먼트 (`/blog/[id]` → `/blog/1`)
- `[...slug]` — 캐치올 (여러 세그먼트)
- `[[...slug]]` — 옵셔널 캐치올

```jsx
// app/blog/[id]/page.tsx
export default function Post({ params }) {
  return <h1>글 {params.id}</h1>;
}
```

## 중첩 레이아웃과 특수 파일

- `layout.tsx` — 하위 경로가 공유하는 레이아웃(이동해도 유지, 상태 보존)
- `loading.tsx` / `error.tsx` / `not-found.tsx` — 로딩·에러·404 UI
- 폴더명을 `(group)`으로 감싸면 URL에 영향 없이 **그룹핑**만 합니다.

## 클라이언트 내비게이션

`next/link`의 `<Link>`로 이동하면 **풀 리로드 없이** 필요한 부분만 가져와 전환합니다(프리페치 포함). 코드에서 이동할 땐 `useRouter().push()`를 씁니다.

```jsx
import Link from 'next/link';
<Link href="/about">소개</Link>;
```

<Quiz
  client:visible
  question="Next.js의 파일 기반 라우팅에 대한 설명으로 옳은 것은?"
  options={[
    {
      text: '폴더 구조가 곧 URL 경로가 되고, [id]처럼 대괄호 폴더로 동적 경로를 만든다.',
      correct: true,
      explanation: 'Next.js는 폴더/파일 구조가 라우트가 되며, 대괄호 표기로 동적 세그먼트를 정의합니다.',
    },
    {
      text: '모든 경로를 routes.config.js 파일에 직접 등록해야 한다.',
      explanation: '별도 라우트 설정 파일이 필요 없습니다. 폴더 구조 자체가 경로가 됩니다.',
    },
    {
      text: '〈Link〉로 이동하면 항상 페이지 전체를 새로고침한다.',
      explanation: '〈Link〉는 풀 리로드 없이 클라이언트 내비게이션으로 필요한 부분만 전환합니다.',
    },
  ]}
/>
