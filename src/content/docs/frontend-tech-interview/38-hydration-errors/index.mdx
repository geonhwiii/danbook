---
title: Next.js의 하이드레이션 오류란 무엇인가요?
description: 서버가 만든 HTML과 클라이언트가 처음 렌더한 결과가 어긋날 때 발생하는 하이드레이션 불일치의 원인과 해결법을 정리합니다.
category: Next.js 핵심 개념
order: 8
---

import Quiz from '@components/react/Quiz.tsx';

**하이드레이션(hydration)** 은 서버가 보낸 정적 HTML에, 클라이언트의 리액트가 **이벤트 핸들러와 상태를 붙여 상호작용 가능하게 만드는** 과정입니다.

## 하이드레이션 오류란

리액트는 하이드레이션 때 **서버가 만든 HTML과 클라이언트의 첫 렌더 결과가 같다고 가정**합니다. 둘이 다르면 "Hydration failed..." 경고가 나고, 해당 부분을 클라이언트 기준으로 다시 그립니다. 이를 **하이드레이션 불일치(mismatch)** 라고 합니다.

## 흔한 원인

서버와 클라이언트에서 **다른 결과를 만드는 코드**가 원인입니다.

- `window`·`localStorage` 등 **브라우저 전용 API**를 렌더링 중 사용
- `Date.now()`, `Math.random()` 처럼 **매번 달라지는 값**을 렌더에 사용
- `typeof window !== 'undefined'`로 서버/클라이언트 분기해 다른 마크업 출력
- `<p>` 안에 `<div>`를 넣는 등 **잘못된 HTML 중첩**(브라우저가 자동 교정)
- 브라우저 확장 프로그램이 DOM을 변형

## 해결 방법

- 브라우저 전용 로직은 **`useEffect` 안에서** 실행 (effect는 클라이언트에서만 동작)
- 클라이언트에서만 달라지는 값은 마운트 후 `useState`로 반영

```jsx
'use client';
function Time() {
  const [now, setNow] = useState(null);
  useEffect(() => setNow(new Date().toLocaleTimeString()), []);
  return <span>{now ?? '로딩...'}</span>; // 서버/첫 렌더는 동일하게
}
```

- 정말 클라이언트에서만 그려야 하면 **동적 임포트**로 SSR을 끕니다: `dynamic(() => import('./C'), { ssr: false })`
- 올바른 HTML 중첩 규칙을 지킵니다.

<Quiz
  client:visible
  question="Next.js 하이드레이션 오류가 발생하는 대표적 원인은?"
  options={[
    {
      text: '서버 렌더링 결과와 클라이언트의 첫 렌더 결과가 달라서(예: 렌더 중 window·Math.random 사용).',
      correct: true,
      explanation:
        '하이드레이션은 서버 HTML과 클라이언트 첫 렌더가 같다고 가정합니다. 둘이 어긋나면 불일치 오류가 납니다.',
    },
    {
      text: 'CSS 파일을 너무 많이 불러왔기 때문이다.',
      explanation: 'CSS 개수는 하이드레이션 불일치의 원인이 아닙니다. 원인은 서버/클라이언트 렌더 결과의 차이입니다.',
    },
    {
      text: 'API 응답이 200이 아닌 상태 코드를 반환했기 때문이다.',
      explanation: 'HTTP 상태 코드와 하이드레이션 불일치는 직접적 관련이 없습니다.',
    },
  ]}
/>
